CupCast final project summary

Project root:
/Users/joe/Desktop/McGill/projects/FIFAproject2026

Current default model:
- V2 player-impact probability model is now the default.
- V1 anchored_final_strength remains the base team-strength layer.
- V2 does not replace team strength directly; it post-processes W/D/L probabilities.
- Active player-impact layers: squad_uncertainty, superstar, club_form.
- Config: config/model_params_default.json
- Model version: v2_uncertainty_superstar_club_form

Prediction/simulation flow:
Raw team + player data
  -> anchored team strength
  -> calibrated Poisson W/D/L probabilities
  -> V2 player-impact probability stack
  -> deterministic prediction outputs and Monte Carlo simulation outputs

API:
- Read-only FastAPI server added under src/api/.
- It only reads existing files from output/.
- It does not fetch ESPN, run evaluation, generate predictions, run tuning, or run simulations.
- Run with: uvicorn src.api.main:app --reload
- Swagger: http://127.0.0.1:8000/docs

Main API endpoints:
- GET /health
- GET /model/summary
- GET /predictions/group-stage
- GET /predictions/matches
- GET /predictions/teams
- GET /predictions/group-stage/{match_id}
- GET /live/results
- GET /live/standings
- GET /live/evaluation
- GET /simulation/tournament
- GET /simulation/teams
- GET /simulation/team/{team}
- GET /bracket/sample

API handoff package:
- deliverables/cupcast_api_package.zip

Recent squad update:
- Japan squad source updated: Wataru Endo removed, Shuto Machino added.
- Player-derived squad value, superstar, club-form, and prediction outputs were rebuilt.

Output organization:
- output/predictions/: deterministic frontend prediction CSVs
- output/simulations/: Monte Carlo simulation CSVs
- output/diagnostics/: model comparison and quality reports
- output/brackets/: sample bracket outputs
- output/live/: ESPN/live validation outputs
- output/reports/: report artifacts

Validation performed:
- API modules py_compile passed.
- API smoke tests passed for list and detail endpoints.
- Prediction outputs include 72 group-stage matches.
- Simulation outputs include V2 player-impact metadata.
